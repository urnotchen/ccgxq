
该权限模块:

    一般情况不作为 composer 包进行依赖引入;
    仅具有管理 role, assignment 的功能;

使用:

    拷贝无版本管理的代码到yii 2 应用中的modules 目录下;
    调整命名空间;
    初始化该模块数据库;
    在 components/Rights 中定义权限, 然后以 `创建 migration 并应用之` 的方式设置权限;
    在特定执行时机进行权限访问判断;
    自定义使用;
